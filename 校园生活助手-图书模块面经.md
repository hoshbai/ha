# 校园生活助手-图书模块技术面经

## 目录
- [1. JWT认证流程详解](#1-jwt认证流程详解)
- [2. ON DUPLICATE KEY UPDATE原理](#2-on-duplicate-key-update原理)
- [3. 软删除机制实现](#3-软删除机制实现)
- [4. 数据库索引优化](#4-数据库索引优化)
- [5. 收藏防重复机制](#5-收藏防重复机制)
- [6. 登录/未登录兼容设计](#6-登录未登录兼容设计)
- [7. MyBatis注解式开发](#7-mybatis注解式开发)
- [8. RESTful API设计规范](#8-restful-api设计规范)
- [9. 常见面试问题](#9-常见面试问题)

---

## 1. JWT认证流程详解

### 1.1 什么是JWT？
JWT (JSON Web Token) 是一种基于JSON的开放标准（RFC 7519），用于在各方之间安全地传输信息。它由三部分组成：
- **Header（头部）**：包含token类型和加密算法
- **Payload（负载）**：包含用户信息（claims），如用户名、过期时间等
- **Signature（签名）**：使用密钥对前两部分进行签名，防止篡改

### 1.2 项目中的JWT认证流程

#### 完整流程图
```
用户登录 → 生成JWT → 前端存储 → 后续请求携带 → 后端验证 → 返回数据
```

#### 详细步骤

**步骤1：用户登录，生成JWT**
```java
// 登录成功后，服务端生成JWT
String token = JwtUtil.generateToken(username);
// token有效期：24小时（在application.yml中配置）
```

**步骤2：前端存储Token**
```java
// Android端使用SharedPreferences存储
SharedPreferences prefs = getSharedPreferences("app_prefs", MODE_PRIVATE);
prefs.edit().putString("token", token).apply();
```

**步骤3：后续请求携带Token**
```java
// 前端从SharedPreferences读取token
String token = prefs.getString("token", null);

// 在请求头中添加Authorization
Request request = new Request.Builder()
    .url(url)
    .addHeader("Authorization", "Bearer " + token)
    .build();
```

**步骤4：后端验证Token**
```java
// LibraryController.java中的典型验证流程
@GetMapping("/details/{id}")
public ResponseEntity<?> getBookDetails(
    @PathVariable Long id,
    @RequestHeader(value = "Authorization", required = false) String authHeader
) {
    // 1. 提取token
    String token = null;
    if (authHeader != null && authHeader.startsWith("Bearer ")) {
        token = authHeader.substring(7);
    }

    // 2. 解析token获取用户名
    String username = JwtUtil.extractUsername(token);

    // 3. 根据用户名查询用户ID
    User user = userMapper.selectByNameOnly(username);

    // 4. 验证失败返回401
    if (user == null) {
        return ResponseEntity.status(401).body("用户未认证");
    }

    // 5. 验证成功，继续业务逻辑
    Long userId = user.getId();
    // ...
}
```

### 1.3 JWT的优势
- **无状态**：服务端不需要存储session，适合分布式系统
- **跨域友好**：可以在HTTP Header中传输，支持跨域请求
- **性能好**：不需要每次请求都查询数据库验证session

### 1.4 JWT的安全性考虑
- **密钥管理**：密钥存储在application.yml中，生产环境应使用环境变量或配置中心
- **HTTPS传输**：token应通过HTTPS传输，防止中间人攻击
- **过期时间**：设置24小时过期，平衡安全性与用户体验
- **敏感信息**：Payload不应存储密码等敏感信息（Base64可解码）

### 1.5 面试可能追问的问题

**Q1: JWT和Session的区别？**
- **存储位置**：Session存在服务端内存/数据库，JWT存在客户端
- **扩展性**：Session需要共享存储（Redis），JWT天然支持分布式
- **性能**：JWT每次请求都需要解析验证，Session只需查询一次
- **安全性**：Session可以主动废除，JWT在过期前无法主动废除

**Q2: 如果JWT被盗用怎么办？**
- 设置较短的过期时间
- 使用HTTPS防止传输过程被截获
- 引入Refresh Token机制（访问token短期，刷新token长期）
- 敏感操作要求二次验证（如修改密码）

**Q3: 为什么不把用户所有信息都放在JWT里？**
- Payload会被Base64编码（不是加密），可被解码查看
- Token越大，传输开销越大
- 用户信息变更时，token需要重新生成

---

## 2. ON DUPLICATE KEY UPDATE原理

### 2.1 业务场景
阅读历史记录需求：
- 用户首次打开图书详情页 → 插入一条阅读记录
- 用户再次打开同一本书 → 更新阅读时间，累加阅读次数
- 避免产生重复记录

### 2.2 SQL实现

#### 项目中的实际SQL
```sql
INSERT INTO reading_history (user_id, book_id, last_read_time, read_count)
VALUES (#{userId}, #{bookId}, NOW(), 1)
ON DUPLICATE KEY UPDATE
    last_read_time = NOW(),
    read_count = read_count + 1
```

#### 工作原理
1. **尝试插入**：MySQL首先尝试执行INSERT操作
2. **检测冲突**：如果主键或唯一索引冲突（user_id + book_id已存在）
3. **执行更新**：触发UPDATE操作，更新指定字段

### 2.3 表结构设计关键

```sql
CREATE TABLE reading_history (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    user_id BIGINT NOT NULL,
    book_id BIGINT NOT NULL,
    last_read_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    read_count INT DEFAULT 1,
    UNIQUE KEY uk_user_book (user_id, book_id)  -- 复合唯一索引是关键
);
```

**核心要点**：
- 必须有主键或唯一索引，否则ON DUPLICATE KEY UPDATE无法工作
- 使用`uk_user_book (user_id, book_id)`保证一个用户对一本书只有一条记录

### 2.4 与其他方案对比

#### 方案1：先SELECT再INSERT/UPDATE（传统方式）
```java
// 需要两次数据库交互
ReadingHistory history = mapper.selectByUserAndBook(userId, bookId);
if (history == null) {
    mapper.insert(new ReadingHistory(userId, bookId));
} else {
    mapper.updateReadCount(history.getId());
}
```
**缺点**：
- 两次数据库交互，性能差
- 高并发下可能产生重复记录（线程A查询后、插入前，线程B也查询到不存在）

#### 方案2：使用事务+锁
```java
@Transactional
public void recordReading(Long userId, Long bookId) {
    // SELECT ... FOR UPDATE 锁行
    ReadingHistory history = mapper.selectForUpdate(userId, bookId);
    // ...
}
```
**缺点**：
- 需要行锁，并发性能下降
- 代码复杂度高

#### 方案3：ON DUPLICATE KEY UPDATE（本项目采用）
```sql
-- 一条SQL搞定，MySQL原子操作
INSERT ... ON DUPLICATE KEY UPDATE ...
```
**优点**：
- 一次数据库交互
- MySQL保证原子性，无需手动加锁
- 代码简洁，性能好

### 2.5 注意事项

**自增主键陷阱**
```sql
-- 即使触发UPDATE，AUTO_INCREMENT仍会递增
-- 长期运行可能导致主键不连续，但不影响功能
```

**更新值的引用**
```sql
-- ✅ 正确：引用旧值累加
read_count = read_count + 1

-- ❌ 错误：直接赋值会覆盖
read_count = 1
```

**批量插入的应用**
```sql
-- 支持批量操作
INSERT INTO reading_history (user_id, book_id, last_read_time, read_count)
VALUES
    (1, 101, NOW(), 1),
    (1, 102, NOW(), 1),
    (2, 101, NOW(), 1)
ON DUPLICATE KEY UPDATE
    last_read_time = NOW(),
    read_count = read_count + 1;
```

### 2.6 面试可能追问的问题

**Q1: ON DUPLICATE KEY UPDATE在高并发下会有问题吗？**
- MySQL会对涉及的行加锁，保证原子性
- 同一user_id+book_id的并发请求会串行执行，不会丢失更新
- 性能优于应用层加锁

**Q2: 如果有多个唯一索引怎么办？**
- 只要任意一个唯一索引冲突，就会触发UPDATE
- 建议只在需要去重的字段上建唯一索引

**Q3: 为什么不用REPLACE INTO？**
```sql
REPLACE INTO reading_history (user_id, book_id, last_read_time, read_count)
VALUES (#{userId}, #{bookId}, NOW(), 1);
```
- REPLACE会先DELETE再INSERT，主键ID会变化
- 如果有外键关联，会触发级联删除
- ON DUPLICATE KEY UPDATE仅更新指定字段，更可控

---

## 3. 软删除机制实现

### 3.1 什么是软删除？
软删除（Soft Delete）不直接从数据库删除数据，而是通过标记字段（如`del_flg`）标识数据已删除，查询时过滤掉这些数据。

### 3.2 项目实现

#### 表结构设计
```sql
CREATE TABLE book_info (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    book_name VARCHAR(200),
    author VARCHAR(100),
    -- 其他字段...
    del_flg TINYINT DEFAULT 0,  -- 0:正常 1:已删除
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);
```

#### 查询时过滤
```java
// LibraryMapper.java中的所有查询
@Select("SELECT * FROM book_info WHERE id = #{id} AND del_flg = 0")
Book getBookDetailsById(@Param("id") Long id);

@Select("SELECT * FROM book_info WHERE del_flg = 0 ORDER BY id DESC")
List<Book> getAllBooks();
```

#### 删除操作
```java
// 管理员删除图书（软删除）
@Update("UPDATE book_info SET del_flg = 1 WHERE id = #{id}")
void softDeleteBook(@Param("id") Long id);

// 如需硬删除（慎用）
@Delete("DELETE FROM book_info WHERE id = #{id}")
void hardDeleteBook(@Param("id") Long id);
```

### 3.3 软删除的优势

| 对比项 | 硬删除（DELETE） | 软删除（UPDATE） |
|--------|------------------|------------------|
| 数据恢复 | 困难（需数据库备份） | 简单（修改标记即可） |
| 审计追溯 | 数据已丢失 | 可追溯删除时间、操作人 |
| 外键处理 | 可能级联删除关联数据 | 不影响关联数据 |
| 性能 | 直接删除，性能好 | 数据持续增长，需定期归档 |
| 业务风险 | 误删无法恢复 | 可设置恢复机制 |

### 3.4 注意事项

**索引失效问题**
```sql
-- ❌ 不推荐：del_flg会导致索引部分失效
SELECT * FROM book_info WHERE category_id = 1 AND del_flg = 0;

-- ✅ 建议：创建联合索引
CREATE INDEX idx_category_del ON book_info(category_id, del_flg);
```

**唯一索引冲突**
```sql
-- 场景：图书ISBN应唯一，删除后重新添加会冲突
CREATE UNIQUE INDEX uk_isbn ON book_info(isbn);

-- 解决方案1：唯一索引包含del_flg
CREATE UNIQUE INDEX uk_isbn_del ON book_info(isbn, del_flg);

-- 解决方案2：删除时修改ISBN值
UPDATE book_info SET del_flg = 1, isbn = CONCAT(isbn, '_deleted_', id) WHERE id = 1;
```

**数据归档策略**
```sql
-- 定期将软删除数据迁移到归档表
INSERT INTO book_info_archive SELECT * FROM book_info WHERE del_flg = 1 AND updated_at < DATE_SUB(NOW(), INTERVAL 1 YEAR);
DELETE FROM book_info WHERE del_flg = 1 AND updated_at < DATE_SUB(NOW(), INTERVAL 1 YEAR);
```

### 3.5 面试可能追问的问题

**Q1: 什么场景适合用软删除？**
- 涉及财务、法律相关数据（需审计）
- 用户数据（隐私合规要求保留删除记录）
- 核心业务数据（防止误删）

**Q2: 软删除的缺点是什么？**
- 数据量持续增长，查询性能下降
- 需要在所有查询中添加del_flg过滤条件
- 唯一索引需要特殊处理

**Q3: 如何防止忘记添加del_flg过滤条件？**
- 使用MyBatis拦截器自动添加条件
- 创建数据库视图，视图中自带过滤条件
- 代码Review时重点检查

---

## 4. 数据库索引优化

### 4.1 项目中的索引设计

#### 索引1：category_id索引
```sql
-- 场景：用户按分类浏览图书（高频操作）
CREATE INDEX idx_category_id ON book_info(category_id);

-- 对应查询
SELECT * FROM book_info WHERE category_id = 3 AND del_flg = 0;
```

**优化效果分析**：
```
未创建索引前：全表扫描，10000条数据耗时~50ms
创建索引后：索引扫描，同样数据耗时~5ms（提升90%）
```

#### 索引2：category表唯一索引
```sql
-- 防止分类名重复
CREATE UNIQUE INDEX uq_category_name ON category(name);
```

**作用**：
- 数据完整性约束（数据库层面防重）
- 按name查询时自动使用索引

#### 索引3：reading_history复合唯一索引
```sql
CREATE UNIQUE INDEX uk_user_book ON reading_history(user_id, book_id);
```

**双重作用**：
- 保证一个用户对一本书只有一条记录
- 加速查询`WHERE user_id = ? AND book_id = ?`

### 4.2 索引设计原则

#### 何时需要创建索引？
- **高频查询字段**：WHERE、JOIN、ORDER BY中常用字段
- **区分度高的字段**：性别（只有2个值）不适合建索引，用户ID适合
- **外键字段**：如category_id关联category表

#### 何时不需要索引？
- **小表**：几百条数据的表，全表扫描更快
- **频繁更新的字段**：每次UPDATE都需要维护索引
- **低区分度字段**：如性别、状态（只有0/1）

### 4.3 联合索引的最左前缀原则

#### 示例
```sql
-- 创建联合索引
CREATE INDEX idx_category_author ON book_info(category_id, author);

-- ✅ 可以使用索引
WHERE category_id = 1
WHERE category_id = 1 AND author = '鲁迅'

-- ❌ 无法使用索引（缺少最左字段category_id）
WHERE author = '鲁迅'
```

#### 本项目的应用
```sql
-- 如果需要支持以下三种查询：
-- 1. WHERE category_id = ?
-- 2. WHERE category_id = ? AND del_flg = ?
-- 3. WHERE del_flg = ?

-- 方案1：创建两个索引
CREATE INDEX idx_category ON book_info(category_id);
CREATE INDEX idx_del ON book_info(del_flg);

-- 方案2：创建一个联合索引（推荐）
CREATE INDEX idx_category_del ON book_info(category_id, del_flg);
-- 可满足查询1、2，查询3需单独索引
```

### 4.4 索引失效场景

```sql
-- ❌ 场景1：使用函数
WHERE YEAR(created_at) = 2024
-- ✅ 改进
WHERE created_at >= '2024-01-01' AND created_at < '2025-01-01'

-- ❌ 场景2：隐式类型转换
WHERE book_id = '123'  -- book_id是BIGINT类型
-- ✅ 改进
WHERE book_id = 123

-- ❌ 场景3：LIKE左模糊
WHERE book_name LIKE '%高等数学%'
-- ✅ 改进（仅右模糊可用索引）
WHERE book_name LIKE '高等数学%'

-- ❌ 场景4：OR条件（部分字段无索引）
WHERE category_id = 1 OR author = '鲁迅'  -- author无索引
-- ✅ 改进：都加索引或改用UNION
```

### 4.5 EXPLAIN分析工具

```sql
EXPLAIN SELECT * FROM book_info WHERE category_id = 3 AND del_flg = 0;
```

**重点关注字段**：
| 字段 | 说明 | 期望值 |
|------|------|--------|
| type | 访问类型 | ref、range、index（避免ALL全表扫描） |
| key | 实际使用的索引 | 应使用预期索引 |
| rows | 扫描行数 | 越少越好 |
| Extra | 额外信息 | 避免Using filesort、Using temporary |

### 4.6 面试可能追问的问题

**Q1: 索引越多越好吗？**
- 否。索引占用存储空间
- INSERT/UPDATE/DELETE需要维护索引，影响写性能
- 一般建议单表索引不超过5个

**Q2: 聚簇索引和非聚簇索引的区别？**
- **聚簇索引**（InnoDB主键）：数据按索引顺序存储，一张表只有一个
- **非聚簇索引**（二级索引）：索引和数据分开存储，需回表查询
- 本项目中，主键id是聚簇索引，category_id是非聚簇索引

**Q3: 为什么不给所有WHERE条件字段都加索引？**
- 索引维护成本高
- MySQL优化器可能选择不使用索引（数据分布、统计信息等因素）
- 需根据实际查询频率和数据量权衡

---

## 5. 收藏防重复机制

### 5.1 业务场景
用户点击收藏按钮时，需要：
- 如果未收藏 → 添加收藏
- 如果已收藏 → 取消收藏
- 防止重复收藏导致数据库主键冲突

### 5.2 实现方案

#### 表结构
```sql
CREATE TABLE favorite_books (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    user_id BIGINT NOT NULL,
    book_id BIGINT NOT NULL,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    UNIQUE KEY uk_user_book (user_id, book_id)  -- 唯一索引保证不重复
);
```

#### 后端代码
```java
// LibraryController.java
@PostMapping("/favorite")
public ResponseEntity<?> toggleFavorite(
    @RequestParam Long bookId,
    @RequestParam String action,  // "add" 或 "remove"
    @RequestHeader("Authorization") String authHeader
) {
    // 1. 验证token，获取userId
    Long userId = getUserIdFromToken(authHeader);

    if ("add".equals(action)) {
        // 2. 先检查是否已收藏
        if (libraryMapper.isFavorite(userId, bookId)) {
            return ResponseEntity.badRequest().body("已收藏该图书");
        }
        // 3. 添加收藏
        libraryMapper.addFavorite(userId, bookId);
    } else if ("remove".equals(action)) {
        // 4. 取消收藏
        libraryMapper.removeFavorite(userId, bookId);
    }

    return ResponseEntity.ok("操作成功");
}
```

#### Mapper实现
```java
// LibraryMapper.java
@Select("SELECT COUNT(*) > 0 FROM favorite_books WHERE user_id = #{userId} AND book_id = #{bookId}")
boolean isFavorite(@Param("userId") Long userId, @Param("bookId") Long bookId);

@Insert("INSERT INTO favorite_books (user_id, book_id) VALUES (#{userId}, #{bookId})")
void addFavorite(@Param("userId") Long userId, @Param("bookId") Long bookId);

@Delete("DELETE FROM favorite_books WHERE user_id = #{userId} AND book_id = #{bookId}")
void removeFavorite(@Param("userId") Long userId, @Param("bookId") Long bookId);
```

### 5.3 为什么需要先查询再插入？

#### 方案对比

**方案1：直接插入（不检查）**
```java
try {
    libraryMapper.addFavorite(userId, bookId);
} catch (DuplicateKeyException e) {
    return ResponseEntity.badRequest().body("已收藏");
}
```
**缺点**：
- 依赖异常处理控制流程（不规范）
- 每次重复操作都抛异常，性能差
- 前端无法预知操作结果

**方案2：先查询再操作（本项目采用）**
```java
if (libraryMapper.isFavorite(userId, bookId)) {
    return ResponseEntity.badRequest().body("已收藏");
}
libraryMapper.addFavorite(userId, bookId);
```
**优点**：
- 流程清晰，易于维护
- 提前反馈给用户，体验好
- 避免不必要的数据库操作

**方案3：数据库唯一索引兜底（最佳实践）**
```java
// 代码层面检查
if (isFavorite(...)) { return error; }
addFavorite(...);

// 数据库层面防御
CREATE UNIQUE INDEX uk_user_book ON favorite_books(user_id, book_id);
```
**双重保障**：
- 正常流程不会重复插入
- 极端情况（高并发）由数据库唯一索引兜底

### 5.4 高并发下的并发问题

#### 问题场景
```
时间轴：
T1: 线程A查询 → 未收藏
T2: 线程B查询 → 未收藏
T3: 线程A插入 → 成功
T4: 线程B插入 → 主键冲突！
```

#### 解决方案

**方案1：数据库唯一索引（本项目采用）**
- 唯一索引保证最终一致性
- 一个线程插入成功，另一个线程抛异常
- 适合低并发场景

**方案2：分布式锁**
```java
String lockKey = "favorite:" + userId + ":" + bookId;
if (redisLock.tryLock(lockKey)) {
    try {
        if (!isFavorite(...)) {
            addFavorite(...);
        }
    } finally {
        redisLock.unlock(lockKey);
    }
}
```
- 适合高并发场景
- 需要引入Redis

**方案3：数据库悲观锁**
```sql
SELECT * FROM favorite_books
WHERE user_id = #{userId} AND book_id = #{bookId}
FOR UPDATE;
```
- 锁定查询的行，其他事务等待
- 性能较差，不推荐

### 5.5 面试可能追问的问题

**Q1: 为什么不用@Transactional注解？**
```java
@Transactional
public void addFavorite(...) {
    if (isFavorite(...)) { throw exception; }
    libraryMapper.addFavorite(...);
}
```
- 对于单条INSERT/DELETE，MySQL本身保证原子性
- 事务主要用于多步操作需要一起成功或失败
- 过度使用事务会增加锁开销

**Q2: 如果改用NoSQL（如MongoDB）怎么实现防重？**
```javascript
db.favorites.createIndex(
    { "userId": 1, "bookId": 1 },
    { unique: true }
)

// 插入时捕获重复键异常
try {
    db.favorites.insertOne({ userId, bookId, createdAt: new Date() })
} catch (DuplicateKeyError) {
    return "已收藏";
}
```

**Q3: 如何统计图书被收藏次数？**
```sql
-- 方案1：实时统计（简单但慢）
SELECT COUNT(*) FROM favorite_books WHERE book_id = #{bookId}

-- 方案2：冗余字段（本项目未采用，但更优）
-- book_info表增加favorite_count字段
-- 收藏时：UPDATE book_info SET favorite_count = favorite_count + 1
-- 取消收藏时：UPDATE book_info SET favorite_count = favorite_count - 1
```

---

## 6. 登录/未登录兼容设计

### 6.1 业务需求
- 未登录用户：可浏览图书详情（书名、作者、简介等）
- 已登录用户：额外显示收藏状态、个人阅读次数

### 6.2 实现方案

#### 后端接口设计
```java
@GetMapping("/details/{id}")
public ResponseEntity<?> getBookDetails(
    @PathVariable Long id,
    @RequestHeader(value = "Authorization", required = false) String authHeader
) {
    // 1. 判断是否登录
    String token = extractToken(authHeader);

    if (token == null) {
        // 2. 未登录：返回基础信息
        Book book = libraryMapper.getBookDetailsById(id);
        return ResponseEntity.ok(book);
    } else {
        // 3. 已登录：返回基础信息 + 个人数据
        Long userId = getUserIdFromToken(token);
        BookDetail detail = libraryMapper.getBookDetailsByIdWithUser(id, userId);
        return ResponseEntity.ok(detail);
    }
}
```

#### Mapper层实现

**未登录查询（仅基础信息）**
```java
@Select("SELECT id, book_name AS bookName, author, publisher, isbn, " +
        "price, summary, cover_image AS coverImage, category_id AS categoryId " +
        "FROM book_info WHERE id = #{id} AND del_flg = 0")
Book getBookDetailsById(@Param("id") Long id);
```

**已登录查询（基础信息 + 个人数据）**
```java
@Select("SELECT b.id, b.book_name AS bookName, b.author, b.publisher, " +
        "b.isbn, b.price, b.summary, b.cover_image AS coverImage, " +
        "b.category_id AS categoryId, " +
        "IF(f.id IS NOT NULL, 1, 0) AS isFavorite, " +  // 是否收藏
        "IFNULL(r.read_count, 0) AS readCount " +        // 阅读次数
        "FROM book_info b " +
        "LEFT JOIN favorite_books f ON b.id = f.book_id AND f.user_id = #{userId} " +
        "LEFT JOIN reading_history r ON b.id = r.book_id AND r.user_id = #{userId} " +
        "WHERE b.id = #{bookId} AND b.del_flg = 0")
BookDetail getBookDetailsByIdWithUser(@Param("bookId") Long bookId, @Param("userId") Long userId);
```

### 6.3 SQL关键技术点

#### LEFT JOIN的作用
```sql
-- 场景：用户可能没有收藏/阅读记录，但仍需返回图书信息
FROM book_info b
LEFT JOIN favorite_books f ON b.id = f.book_id AND f.user_id = 123
```
- **INNER JOIN**：只返回两表都匹配的行（用户未收藏时，整行不返回）
- **LEFT JOIN**：保留左表所有行（用户未收藏时，f字段为NULL）

#### IF函数判断收藏状态
```sql
IF(f.id IS NOT NULL, 1, 0) AS isFavorite
-- 逻辑：如果JOIN到收藏记录，返回1（已收藏），否则返回0（未收藏）
```

#### IFNULL处理空值
```sql
IFNULL(r.read_count, 0) AS readCount
-- 逻辑：如果用户没有阅读记录，r.read_count为NULL，替换为0
```

### 6.4 前端处理

#### Android端代码
```java
// BookDetailActivity.java
private void loadBookDetail() {
    String token = getToken();  // 从SharedPreferences读取

    ApiService api = RetrofitClient.getInstance().getApi();
    Call<Book> call;

    if (token == null) {
        // 未登录：不传Authorization头
        call = api.getBookDetail(bookId);
    } else {
        // 已登录：传Authorization头
        call = api.getBookDetailWithAuth("Bearer " + token, bookId);
    }

    call.enqueue(new Callback<Book>() {
        @Override
        public void onResponse(Call<Book> call, Response<Book> response) {
            if (response.isSuccessful()) {
                Book book = response.body();
                displayBook(book);

                // 根据登录状态显示/隐藏收藏按钮
                if (token != null) {
                    favoriteButton.setVisibility(View.VISIBLE);
                    updateFavoriteIcon(book.isFavorite());
                } else {
                    favoriteButton.setVisibility(View.GONE);
                }
            }
        }
    });
}
```

### 6.5 优势分析

#### 对比方案1：强制登录
```java
// 所有接口都要求登录
if (token == null) {
    return ResponseEntity.status(401).body("请先登录");
}
```
**缺点**：
- 用户体验差（只想看看也要注册登录）
- 流失潜在用户

#### 对比方案2：前端判断（不推荐）
```java
// 后端总是返回完整数据，前端根据登录状态决定显示什么
// ❌ 问题：未登录用户能通过抓包看到其他用户的收藏状态（安全隐患）
```

#### 本项目方案优势
- **用户体验好**：游客可浏览，想收藏再注册
- **数据安全**：后端控制返回字段，不泄露个人数据
- **性能优化**：未登录时不查询收藏/历史表，减少JOIN开销

### 6.6 面试可能追问的问题

**Q1: Authorization头设置required = false有什么风险？**
- 需在代码中显式判断token是否存在
- 容易忘记验证导致权限漏洞
- 建议：敏感操作（如删除）必须required = true

**Q2: 如果未登录用户量很大，怎么优化？**
- 未登录接口加缓存（Redis缓存图书详情）
- CDN缓存静态资源（封面图）
- 读写分离（未登录用户走从库）

**Q3: LEFT JOIN性能会比单表查询慢吗？**
- 会慢，但差异不大（都有索引时）
- 可以分步查询：先查book_info，前端判断登录后再请求收藏状态
- 本项目数据量小，JOIN可接受

---

## 7. MyBatis注解式开发

### 7.1 注解式 vs XML式对比

#### XML方式（传统）
```xml
<!-- LibraryMapper.xml -->
<mapper namespace="com.example.mapper.LibraryMapper">
    <select id="getBookDetailsById" resultType="Book">
        SELECT id, book_name AS bookName, author
        FROM book_info
        WHERE id = #{id} AND del_flg = 0
    </select>
</mapper>
```

#### 注解方式（本项目采用）
```java
// LibraryMapper.java
@Select("SELECT id, book_name AS bookName, author " +
        "FROM book_info WHERE id = #{id} AND del_flg = 0")
Book getBookDetailsById(@Param("id") Long id);
```

#### 优缺点对比

| 对比项 | XML方式 | 注解方式 |
|--------|---------|----------|
| 代码位置 | 分离（接口+XML） | 集中（仅接口） |
| 复杂SQL | 支持良好（动态SQL） | 支持有限（需@SelectProvider） |
| 开发效率 | 需切换文件 | 快速（一个文件） |
| 可读性 | 复杂SQL更清晰 | 简单SQL更直观 |
| 维护性 | 修改需同步接口和XML | 修改仅需改接口 |

### 7.2 常用注解详解

#### @Select - 查询
```java
// 简单查询
@Select("SELECT * FROM book_info WHERE id = #{id}")
Book selectById(Long id);

// 字段映射（数据库snake_case → Java camelCase）
@Select("SELECT book_name AS bookName, cover_image AS coverImage FROM book_info")
List<Book> selectAll();

// 多参数
@Select("SELECT * FROM favorite_books WHERE user_id = #{userId} AND book_id = #{bookId}")
boolean isFavorite(@Param("userId") Long userId, @Param("bookId") Long bookId);
```

#### @Insert - 插入
```java
// 普通插入
@Insert("INSERT INTO favorite_books (user_id, book_id) VALUES (#{userId}, #{bookId})")
void addFavorite(@Param("userId") Long userId, @Param("bookId") Long bookId);

// 返回自增主键
@Insert("INSERT INTO reading_history (user_id, book_id, read_count) VALUES (#{userId}, #{bookId}, 1)")
@Options(useGeneratedKeys = true, keyProperty = "id")
void insertHistory(ReadingHistory history);

// ON DUPLICATE KEY UPDATE
@Insert("INSERT INTO reading_history (user_id, book_id, last_read_time, read_count) " +
        "VALUES (#{userId}, #{bookId}, NOW(), 1) " +
        "ON DUPLICATE KEY UPDATE last_read_time = NOW(), read_count = read_count + 1")
void recordReading(@Param("userId") Long userId, @Param("bookId") Long bookId);
```

#### @Update - 更新
```java
// 更新单个字段
@Update("UPDATE book_info SET status = #{status} WHERE id = #{id}")
void updateBookStatus(@Param("id") Long id, @Param("status") String status);

// 软删除
@Update("UPDATE book_info SET del_flg = 1 WHERE id = #{id}")
void softDeleteBook(@Param("id") Long id);
```

#### @Delete - 删除
```java
// 简单删除
@Delete("DELETE FROM favorite_books WHERE user_id = #{userId} AND book_id = #{bookId}")
void removeFavorite(@Param("userId") Long userId, @Param("bookId") Long bookId);

// 批量删除（不推荐注解方式，应使用XML或@DeleteProvider）
@Delete("DELETE FROM favorite_books WHERE user_id = #{userId}")
void removeAllFavorites(@Param("userId") Long userId);
```

### 7.3 高级用法

#### @Results - 结果映射
```java
@Select("SELECT id, book_name, cover_image, created_at FROM book_info WHERE id = #{id}")
@Results({
    @Result(property = "bookName", column = "book_name"),
    @Result(property = "coverImage", column = "cover_image"),
    @Result(property = "createdAt", column = "created_at")
})
Book getBookById(Long id);
```

#### @SelectProvider - 动态SQL
```java
// 复杂动态SQL场景
public class LibrarySqlProvider {
    public String searchBooks(Map<String, Object> params) {
        StringBuilder sql = new StringBuilder("SELECT * FROM book_info WHERE del_flg = 0");

        if (params.get("keyword") != null) {
            sql.append(" AND (book_name LIKE CONCAT('%', #{keyword}, '%')");
            sql.append(" OR author LIKE CONCAT('%', #{keyword}, '%'))");
        }

        if (params.get("categoryId") != null) {
            sql.append(" AND category_id = #{categoryId}");
        }

        return sql.toString();
    }
}

@SelectProvider(type = LibrarySqlProvider.class, method = "searchBooks")
List<Book> searchBooks(@Param("keyword") String keyword,
                       @Param("categoryId") Long categoryId);
```

### 7.4 注解开发最佳实践

#### 1. 使用@Param明确参数名
```java
// ❌ 不推荐：多参数时MyBatis会用param1、param2
void addFavorite(Long userId, Long bookId);

// ✅ 推荐：使用@Param明确参数名
void addFavorite(@Param("userId") Long userId, @Param("bookId") Long bookId);
```

#### 2. 字段别名映射
```java
// ❌ 不推荐：依赖MyBatis自动映射（不稳定）
@Select("SELECT book_name, cover_image FROM book_info")
List<Book> selectAll();

// ✅ 推荐：显式使用AS别名
@Select("SELECT book_name AS bookName, cover_image AS coverImage FROM book_info")
List<Book> selectAll();
```

#### 3. 复杂SQL使用XML
```java
// 简单SQL：注解
@Select("SELECT * FROM book_info WHERE id = #{id}")
Book selectById(Long id);

// 复杂SQL（多表JOIN、动态条件）：XML
// LibraryMapper.xml
<select id="complexSearch" resultType="Book">
    SELECT b.*, c.name AS categoryName
    FROM book_info b
    LEFT JOIN category c ON b.category_id = c.id
    WHERE b.del_flg = 0
    <if test="keyword != null">
        AND (b.book_name LIKE CONCAT('%', #{keyword}, '%')
        OR b.author LIKE CONCAT('%', #{keyword}, '%'))
    </if>
    ORDER BY b.created_at DESC
</select>
```

### 7.5 面试可能追问的问题

**Q1: #{} 和 ${} 的区别？**
```java
// #{} - 预编译，防SQL注入（推荐）
@Select("SELECT * FROM book_info WHERE id = #{id}")
Book selectById(Long id);
// 生成SQL: SELECT * FROM book_info WHERE id = ?

// ${} - 字符串拼接，有SQL注入风险
@Select("SELECT * FROM book_info ORDER BY ${column}")
List<Book> selectAll(String column);
// 生成SQL: SELECT * FROM book_info ORDER BY book_name
// ⚠️ 危险：用户传入 "id; DROP TABLE book_info--" 会删库
```

**Q2: 如何获取INSERT后的自增主键？**
```java
@Insert("INSERT INTO reading_history (user_id, book_id) VALUES (#{userId}, #{bookId})")
@Options(useGeneratedKeys = true, keyProperty = "id")
void insertHistory(ReadingHistory history);

// 调用后，history.getId()会自动填充主键值
ReadingHistory history = new ReadingHistory(userId, bookId);
mapper.insertHistory(history);
System.out.println(history.getId());  // 输出：自增主键值
```

**Q3: 注解方式如何实现批量操作？**
```java
// 方案1：@InsertProvider动态生成SQL
public class BatchSqlProvider {
    public String batchInsert(Map<String, Object> params) {
        List<ReadingHistory> list = (List<ReadingHistory>) params.get("list");
        StringBuilder sql = new StringBuilder("INSERT INTO reading_history (user_id, book_id) VALUES ");
        for (int i = 0; i < list.size(); i++) {
            sql.append("(#{list[").append(i).append("].userId}, ")
               .append("#{list[").append(i).append("].bookId})");
            if (i < list.size() - 1) sql.append(", ");
        }
        return sql.toString();
    }
}

@InsertProvider(type = BatchSqlProvider.class, method = "batchInsert")
void batchInsert(@Param("list") List<ReadingHistory> list);

// 方案2：循环调用（性能较差，不推荐）
for (ReadingHistory history : list) {
    mapper.insertHistory(history);
}
```

---

## 8. RESTful API设计规范

### 8.1 RESTful核心原则
- **资源导向**：URL表示资源，不是动作
- **HTTP方法语义**：GET查询、POST创建、PUT更新、DELETE删除
- **无状态**：每次请求包含完整信息（通过JWT传递用户身份）

### 8.2 项目中的API设计

#### 路径设计
```java
// ✅ 正确：路径表示资源
GET    /api/library/books              // 获取图书列表
GET    /api/library/books/{id}         // 获取单本图书详情
POST   /api/library/favorite           // 添加/取消收藏
GET    /api/library/favorites          // 获取收藏列表
GET    /api/library/history            // 获取阅读历史

// ❌ 错误：路径包含动作
GET    /api/library/getBooks
POST   /api/library/addFavorite
POST   /api/library/deleteFavorite
```

#### HTTP方法选择

**GET - 查询操作（幂等）**
```java
@GetMapping("/books/{id}")
public ResponseEntity<Book> getBookDetail(@PathVariable Long id) {
    // 多次请求结果一致，不改变服务器状态
}
```

**POST - 创建/非幂等操作**
```java
@PostMapping("/favorite")
public ResponseEntity<?> toggleFavorite(
    @RequestParam Long bookId,
    @RequestParam String action  // "add" 或 "remove"
) {
    // 多次请求可能产生不同结果（添加 → 取消 → 添加）
}
```

**PUT - 更新操作（幂等）**
```java
// 项目中未使用，如需更新图书信息可用
@PutMapping("/books/{id}")
public ResponseEntity<?> updateBook(@PathVariable Long id, @RequestBody Book book) {
    // 多次请求结果一致（更新为同一状态）
}
```

**DELETE - 删除操作（幂等）**
```java
// 项目中使用软删除，实际为UPDATE
@DeleteMapping("/books/{id}")
public ResponseEntity<?> deleteBook(@PathVariable Long id) {
    libraryMapper.softDeleteBook(id);
    // 多次删除同一资源，结果一致（已删除状态）
}
```

### 8.3 响应格式设计

#### 标准响应结构
```java
// 成功响应
{
    "code": 200,
    "message": "操作成功",
    "data": {
        "id": 1,
        "bookName": "高等数学",
        "author": "同济大学"
    }
}

// 失败响应
{
    "code": 400,
    "message": "参数错误：bookId不能为空",
    "data": null
}
```

#### HTTP状态码使用
```java
// 200 OK - 成功
return ResponseEntity.ok(book);

// 201 Created - 创建成功
return ResponseEntity.status(201).body(newBook);

// 400 Bad Request - 请求参数错误
return ResponseEntity.badRequest().body("参数错误");

// 401 Unauthorized - 未认证
return ResponseEntity.status(401).body("请先登录");

// 403 Forbidden - 无权限
return ResponseEntity.status(403).body("无权限访问");

// 404 Not Found - 资源不存在
return ResponseEntity.status(404).body("图书不存在");

// 500 Internal Server Error - 服务器错误
return ResponseEntity.status(500).body("服务器错误");
```

### 8.4 API版本管理

#### 路径版本控制（推荐）
```java
// v1版本
@RequestMapping("/api/v1/library")
public class LibraryControllerV1 { }

// v2版本（新增字段、修改逻辑）
@RequestMapping("/api/v2/library")
public class LibraryControllerV2 { }
```

#### Header版本控制
```java
@GetMapping("/api/library/books")
public ResponseEntity<?> getBooks(
    @RequestHeader(value = "API-Version", defaultValue = "1") String version
) {
    if ("2".equals(version)) {
        // v2逻辑
    } else {
        // v1逻辑
    }
}
```

### 8.5 分页、排序、过滤

#### 分页参数
```java
@GetMapping("/books")
public ResponseEntity<?> getBooks(
    @RequestParam(defaultValue = "1") Integer page,
    @RequestParam(defaultValue = "20") Integer pageSize
) {
    int offset = (page - 1) * pageSize;
    List<Book> books = libraryMapper.selectPage(offset, pageSize);

    return ResponseEntity.ok(Map.of(
        "data", books,
        "page", page,
        "pageSize", pageSize,
        "total", libraryMapper.countBooks()
    ));
}
```

#### 排序参数
```java
// GET /api/library/books?sort=created_at&order=desc
@GetMapping("/books")
public ResponseEntity<?> getBooks(
    @RequestParam(defaultValue = "id") String sort,
    @RequestParam(defaultValue = "asc") String order
) {
    List<Book> books = libraryMapper.selectAllSorted(sort, order);
    return ResponseEntity.ok(books);
}
```

#### 过滤参数
```java
// GET /api/library/books?category=1&keyword=数学
@GetMapping("/books")
public ResponseEntity<?> searchBooks(
    @RequestParam(required = false) Long category,
    @RequestParam(required = false) String keyword
) {
    List<Book> books = libraryMapper.searchBooks(category, keyword);
    return ResponseEntity.ok(books);
}
```

### 8.6 面试可能追问的问题

**Q1: PUT和POST的区别？**
- **PUT**：幂等（多次请求结果一致），用于更新整个资源
- **POST**：非幂等（多次请求可能产生多个资源），用于创建资源
- **PATCH**：部分更新（只更新指定字段）

```java
// PUT - 更新整个图书信息
PUT /api/library/books/1
{
    "bookName": "高等数学（第7版）",
    "author": "同济大学",
    "publisher": "高等教育出版社",
    // 所有字段都要传
}

// PATCH - 仅更新价格
PATCH /api/library/books/1
{
    "price": 39.00
}
```

**Q2: 为什么收藏接口用POST而不是DELETE？**
```java
// 当前设计：POST + action参数
POST /api/library/favorite?bookId=1&action=remove

// RESTful标准设计：
DELETE /api/library/favorites/{bookId}  // 取消收藏
POST   /api/library/favorites           // 添加收藏（Body: {bookId: 1}）
```
- 当前设计为简化前端调用（一个接口处理两个操作）
- 标准RESTful设计语义更清晰，推荐后者

**Q3: 如何设计返回200还是201？**
```java
// 200 OK - 查询、更新、删除成功
GET    /api/library/books/1     → 200
PUT    /api/library/books/1     → 200
DELETE /api/library/books/1     → 200

// 201 Created - 创建成功
POST   /api/library/books       → 201
Location: /api/library/books/123  // 响应头包含新资源URL
```

---

## 9. 常见面试问题

### 9.1 综合性问题

#### Q: 介绍一下你在这个项目中的职责和贡献
**回答思路**：
```
1. 背景介绍（1句话）：
   "这是一个面向大学生的Android校园助手应用，我负责图书管理模块的后端开发"

2. 核心职责（2-3点）：
   - 独立完成4张数据库表设计（book_info、favorite_books等）
   - 使用Spring Boot + MyBatis开发11个RESTful接口
   - 实现JWT身份认证机制，支持登录/未登录用户分权访问

3. 技术亮点（2-3点）：
   - 使用ON DUPLICATE KEY UPDATE解决阅读历史去重问题
   - 通过索引优化（category_id）提升分类查询性能XX%
   - 实现软删除机制保障数据安全

4. 成果数据：
   "目前服务XX名用户，接口响应时间控制在XXms内"
```

#### Q: 项目中遇到的最大挑战是什么，如何解决的？
**示例回答**：
```
挑战：阅读历史记录功能，初期设计是先查询再插入/更新，高并发测试时发现会产生重复记录

原因分析：
- 线程A查询（不存在） → 线程B查询（不存在） → 线程A插入 → 线程B插入（重复）
- 虽然有唯一索引兜底，但异常处理影响性能

解决方案：
- 改用MySQL的ON DUPLICATE KEY UPDATE机制
- 一条SQL完成"存在则更新，不存在则插入"
- 设置user_id+book_id复合唯一索引
- 利用MySQL事务特性保证原子性

效果：
- 代码从两次数据库交互减少到一次
- 完全避免重复记录
- 接口响应时间从XXms降低到XXms
```

### 9.2 技术细节问题

#### Q: 如何保证JWT的安全性？
```
1. 传输安全：
   - 生产环境必须使用HTTPS，防止token被中间人截获
   - token不存储敏感信息（密码、身份证号等）

2. 存储安全：
   - Android端使用SharedPreferences存储（可进一步加密）
   - 服务端密钥存储在配置中心，不硬编码

3. 时效性：
   - 设置24小时过期时间
   - 敏感操作（修改密码）缩短时效或要求重新登录

4. 防止盗用：
   - 可结合设备指纹（生成token时绑定设备ID）
   - 引入Refresh Token机制
```

#### Q: 软删除有什么缺点，如何优化？
```
缺点：
1. 数据持续增长，影响查询性能
2. 唯一索引需要特殊处理（isbn删除后重新添加会冲突）
3. 每个查询都要加del_flg=0条件

优化方案：
1. 定期数据归档：
   DELETE FROM book_info WHERE del_flg=1 AND updated_at < '1年前'

2. 创建视图简化查询：
   CREATE VIEW book_info_active AS
   SELECT * FROM book_info WHERE del_flg = 0

3. 使用MyBatis拦截器自动添加条件：
   @Intercepts({@Signature(...)})
   public class SoftDeleteInterceptor implements Interceptor {
       // 自动在SQL中添加 AND del_flg = 0
   }
```

#### Q: 如果图书数据量达到百万级，如何优化？
```
数据库层面：
1. 分库分表：
   - 按category_id分表（不同分类存不同表）
   - 按id范围分表（1-100万、100万-200万）

2. 读写分离：
   - 主库处理INSERT/UPDATE/DELETE
   - 从库处理SELECT查询

3. 索引优化：
   - 覆盖索引减少回表
   - 联合索引优化多条件查询

应用层面：
4. 缓存策略：
   - 热门图书详情缓存到Redis（TTL 1小时）
   - 分类列表缓存（TTL 24小时）

5. 异步处理：
   - 阅读历史记录改为异步写入（消息队列）
   - 收藏数统计使用定时任务更新

6. 搜索引擎：
   - 图书搜索接入Elasticsearch
   - 支持全文检索、分词、高亮
```

### 9.3 场景设计问题

#### Q: 如何设计图书推荐功能？
```
基于阅读历史的协同过滤：

1. 数据准备：
   - 记录用户阅读行为（已有reading_history表）
   - 提取用户-图书评分矩阵（阅读次数作为权重）

2. 推荐算法（简化版）：
   SQL实现基于分类的推荐：

   -- 查找用户最常阅读的分类
   SELECT category_id, COUNT(*) AS cnt
   FROM reading_history rh
   JOIN book_info b ON rh.book_id = b.id
   WHERE rh.user_id = #{userId}
   GROUP BY category_id
   ORDER BY cnt DESC
   LIMIT 3;

   -- 推荐这些分类下用户未读的热门图书
   SELECT b.*
   FROM book_info b
   WHERE b.category_id IN (...)
     AND b.id NOT IN (SELECT book_id FROM reading_history WHERE user_id = #{userId})
   ORDER BY b.view_count DESC  -- 需增加view_count字段
   LIMIT 10;

3. 进阶方案：
   - 引入机器学习模型（协同过滤、矩阵分解）
   - 使用Spark离线计算推荐结果，存入Redis
   - 实时推荐引擎（Flink实时处理用户行为）
```

#### Q: 如何防止接口被恶意刷量？
```
1. JWT认证：
   - 未登录用户限制访问频率（IP维度）
   - 已登录用户限制操作频率（用户维度）

2. 接口限流：
   @RateLimiter(rate = 100, unit = RateLimitUnit.MINUTE)
   public ResponseEntity<?> getBookDetail() { }

   实现方案：
   - 单机：Guava RateLimiter
   - 分布式：Redis + Lua脚本实现滑动窗口

3. 防刷策略：
   - 图形验证码（频繁操作时要求输入）
   - 设备指纹（检测异常设备）
   - 行为分析（短时间内阅读100本书，可疑）

4. 业务限制：
   - 阅读历史限制最近50条
   - 收藏数量限制（如最多收藏500本）
```

### 9.4 数据库设计问题

#### Q: 为什么favorite_books和reading_history设计成独立表？
```
方案对比：

方案1：冗余字段在book_info表（❌ 不推荐）
book_info:
  - id
  - book_name
  - is_favorite（每个用户不同，无法存储）

方案2：独立关联表（✅ 本项目采用）
favorite_books:
  - user_id
  - book_id

优势：
1. 一对多关系：一本书可被多个用户收藏
2. 查询灵活：
   - 查某用户的收藏：WHERE user_id = ?
   - 查某书被多少人收藏：WHERE book_id = ?
3. 扩展性好：后续可增加收藏时间、备注等字段
```

#### Q: 如果要增加图书评分功能，如何设计？
```
新增表：book_ratings
CREATE TABLE book_ratings (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    user_id BIGINT NOT NULL,
    book_id BIGINT NOT NULL,
    rating TINYINT NOT NULL,  -- 评分1-5星
    review TEXT,              -- 评论内容
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    UNIQUE KEY uk_user_book (user_id, book_id)  -- 一个用户对一本书只能评一次
);

book_info表增加冗余字段（提升查询性能）：
ALTER TABLE book_info ADD COLUMN avg_rating DECIMAL(3,2) DEFAULT 0.00;
ALTER TABLE book_info ADD COLUMN rating_count INT DEFAULT 0;

更新逻辑：
-- 用户提交评分时触发
UPDATE book_info SET
    avg_rating = (SELECT AVG(rating) FROM book_ratings WHERE book_id = #{bookId}),
    rating_count = (SELECT COUNT(*) FROM book_ratings WHERE book_id = #{bookId})
WHERE id = #{bookId};
```

---

## 10. 面试准备建议

### 10.1 准备清单

#### 技术深度
- [ ] 能画出JWT认证的完整流程图
- [ ] 能手写ON DUPLICATE KEY UPDATE的SQL
- [ ] 能解释LEFT JOIN的工作原理
- [ ] 能说出项目中所有索引的作用

#### 业务理解
- [ ] 能说清楚每个接口的业务逻辑
- [ ] 能解释为什么这样设计（而不是其他方案）
- [ ] 能举例说明优化的效果（带数据）

#### 问题应对
- [ ] 准备3个项目中遇到的问题及解决方案
- [ ] 准备2个技术选型的思考过程
- [ ] 准备1个优化案例（前后对比）

### 10.2 STAR法则回答模板

**S**ituation（情境）：项目背景和你的角色
**T**ask（任务）：遇到的具体问题或目标
**A**ction（行动）：你采取的具体行动
**R**esult（结果）：最终的成果和收获

#### 示例：
```
S: 在校园生活助手项目中，我负责图书模块后端开发，需要实现阅读历史记录功能

T: 初期设计发现高并发场景下会产生重复记录，测试环境100并发请求产生了15条重复数据

A:
  1. 分析问题：查询和插入之间的时间窗口导致并发问题
  2. 调研方案：对比了分布式锁、悲观锁、ON DUPLICATE KEY UPDATE
  3. 选择方案：采用ON DUPLICATE KEY UPDATE，一条SQL解决
  4. 实施优化：设置复合唯一索引，修改Mapper SQL语句

R:
  - 完全解决重复记录问题
  - 接口响应时间从平均50ms降低到30ms
  - 学会了数据库原子操作的应用，提升了并发处理能力
```

### 10.3 避坑指南

#### ❌ 不要说的话：
- "这个功能是我独立完成的"（后又说不清楚技术细节）
- "性能提升了很多"（没有具体数据）
- "这个我没仔细看过源码"（对用到的技术不了解）
- "网上找的解决方案"（不知道原理）

#### ✅ 应该说的话：
- "我负责了XX模块，具体包括数据库设计、接口开发、联调测试"
- "通过索引优化，查询时间从50ms降低到5ms，提升了90%"
- "JWT的实现原理是...，我们项目中这样配置..."
- "当时对比了3种方案，最终选择XX因为..."

### 10.4 技术栈关键词（简历&面试）

**后端技术**：
- Spring Boot 2.x（依赖注入、自动配置、RESTful API）
- MyBatis（注解式开发、动态SQL、结果映射）
- MySQL 5.x（索引优化、事务ACID、SQL调优）
- JWT（无状态认证、Token生成与验证）

**数据库设计**：
- 关系建模（一对多、多对多）
- 索引设计（B+Tree、联合索引、唯一索引）
- 查询优化（EXPLAIN分析、覆盖索引、避免全表扫描）
- 事务特性（原子性、ON DUPLICATE KEY UPDATE）

**系统设计**：
- RESTful规范（资源导向、HTTP方法语义）
- 软删除机制（数据安全、审计追溯）
- 分权设计（登录/未登录兼容）
- 防重复机制（唯一索引、先查后插）

**前端交互**：
- Retrofit 2.x（网络请求封装）
- JWT传递（Authorization Header）
- 错误处理（401/400状态码）

---

## 11. 总结

### 核心技术点总结
1. **JWT认证**：无状态、跨域友好、性能好，注意密钥安全和过期时间设置
2. **ON DUPLICATE KEY UPDATE**：解决并发去重问题，一条SQL原子操作，需配合唯一索引
3. **软删除**：数据可恢复、支持审计，注意索引优化和唯一索引处理
4. **索引优化**：高频查询字段建索引，注意最左前缀原则，避免索引失效
5. **RESTful设计**：资源导向、HTTP方法语义、标准响应格式

### 学习路径建议
1. **深入源码**：阅读Spring Boot、MyBatis核心源码，理解依赖注入、动态代理
2. **性能优化**：学习JVM调优、SQL优化、缓存策略
3. **分布式技术**：Redis缓存、消息队列、分库分表
4. **项目实践**：尝试用Elasticsearch改造搜索、用Redis优化热点数据

### 面试高频问题（快速复习）
- JWT工作原理 → Header、Payload、Signature三部分
- ON DUPLICATE KEY UPDATE底层原理 → 尝试INSERT，冲突则UPDATE
- 软删除优缺点 → 可恢复但数据增长，需定期归档
- 索引失效场景 → 函数、类型转换、左模糊、OR无索引
- RESTful PUT vs POST → PUT幂等用于更新，POST非幂等用于创建

---

**祝你面试顺利！** 🚀
