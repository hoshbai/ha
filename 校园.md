请基于以下“校园生活助手-图书功能模块”的完整实现信息，create一段适配技术岗位（后端）、面向应届生/实习生的简历项目经历，要求如下：
1. 结构逻辑：按“项目背景→核心职责→关键实现→项目成果”组织，上下文流畅自然，无逻辑断层；
2. 个人贡献：突出“独立完成模块开发”“从0到1落地功能”，体现应届生/实习生的学习能力、执行能力，明确负责从后端接口开发、数据库设计到与前端Android端联调的完整链路；
3. 细节融入：必须包含以下核心信息——
   - 功能：图书详情查看（登录/未登录兼容）、收藏管理、阅读历史记录（静默记录+去重+50条限制）、多条件搜索、分类浏览、管理员软删除/状态更新；
   - 技术栈：后端（Spring Boot 2.x、MyBatis、MySQL 5.x、JWT）、前端（Android API 21+、Retrofit 2.x、Glide、RecyclerView）、数据库表设计（book_info/favorite_books等4表）；
   - 优化亮点：category_id索引、category表唯一索引、收藏防重复校验、阅读历史ON DUPLICATE KEY UPDATE去重、软删除机制、未登录友好交互；
   - 关键逻辑：JWT身份认证流程、详情接口动态返回数据、阅读历史自动累加次数；
4. 成果呈现：核心数据留占位符（如XX名师生、XX%效率提升、XX个月稳定运行），聚焦“功能落地”“技术应用”“问题解决”，符合应届生/实习生项目成果的真实呈现；
5. 语言风格：专业简洁、务实接地气，避免使用“主导”“架构设计”等超出应届生/实习生职责的表述，符合技术岗位简历表述习惯，不口语化。

【参考信息】
1. 核心功能（已实现的具体功能）

  - 图书详情查看：展示书名、作者、出版社、ISBN、价格、内容简介、封面图等完整信息；支持登录用户查看收藏状态和个人阅读
    次数
    
  - 图书收藏管理：用户可收藏/取消收藏图书，通过心形图标切换状态；收藏列表独立查询展示

  - 阅读历史记录：打开图书详情页自动记录阅读行为，累计阅读次数，按时间倒序展示最近50条历史

  - 图书搜索：支持按书名、作者、分类名三字段模糊搜索，使用LIKE查询

  - 图书分类浏览：按分类标签页展示图书（全部/具体分类），通过category表关联查询

  - 状态管理：管理员可更新图书状态，支持软删除机制（del_flg字段）

      项目背景

      校园生活助手是一款专为大学生设计的 Android 应用程序，旨在帮助学生更高效地管理校园生活。通过该应用，用户可以轻松访问校园内的各种资源和服务，例如课程表、图书馆查询、食堂信息、宿舍管理等。无论是学习、生活还是社交，校园生活助手都能为你提供便利。

      本项目已适配 **Android 12**，充分利用了 Android 12 的新特性（如 Material You 主题、隐私保护增强等），以提供现代化的用户体验。

---
  2. 技术细节（技术栈与实现方式）

     

  后端技术栈：
  - Spring Boot 2.x：RESTful API框架，Controller层提供11个接口
  - MyBatis：数据持久化框架，使用注解方式（@Select、@Insert、@Delete、@Update）编写SQL
  - MySQL 5.x：数据库，端口13306，使用utf8mb4_unicode_ci编码支持中文
  - JWT认证：使用JwtUtil提取用户身份，密钥存储在application.yml，有效期24小时
  - Maven：依赖管理（推测，从包结构判断）

  前端技术栈：
  - Android API 21+：原生Android开发
  - Retrofit 2.x：网络请求库，定义ApiService接口与后端通信
  - Glide：图片加载框架，加载图书封面（BASE_URL: http://10.0.2.2:8081/images/）
  - RecyclerView：图书列表展示，使用网格布局
  - SharedPreferences：本地存储用户token

  数据库设计：
  - book_info表：存储图书基础信息（14个字段），主键自增，包含软删除标识
  - favorite_books表：用户收藏关联表（user_id + book_id组合）
  - reading_history表：阅读历史（user_id + book_id + last_read_time + read_count）
  - category表：分类表（id + name），与book_info通过category_id关联


  接口类型：
  - RESTful风格，路径前缀/api/library
  - HTTP方法：GET（查询）、POST（操作）
  - 响应格式：JSON

---
  3. 业务逻辑（关键流程实现）

  身份验证流程：
  1. 前端从SharedPreferences读取token，在请求头添加Authorization: Bearer {token}
  2. 后端通过JwtUtil.extractUsername(token)解析用户名
  3. 查询UserMapper.selectByNameOnly(username)获取user_id
  4. 若token无效或用户不存在，返回401状态码

  收藏功能流程：
  1. 点击收藏按钮前判断登录状态，未登录跳转登录页（BookDetailActivity.java:128-131）
  2. 前端根据当前状态发送action参数（"add"或"remove"）
  3. 后端先调用isFavorite()检查是否已收藏，避免重复插入（LibraryController.java:83-84）
  4. 执行INSERT或DELETE操作favorite_books表
  5. 前端更新本地Book对象状态，切换图标显示

  阅读历史记录流程：
  1. 用户打开图书详情页时，前端静默调用recordView()接口（BookDetailActivity.java:168-183）
  2. 后端执行SQL：INSERT ... ON DUPLICATE KEY UPDATE last_read_time=NOW(),
    read_count=read_count+1（LibraryMapper.java:86-93）
  3. 首次阅读插入新记录，再次阅读更新时间并累加次数
  4. 查询历史时按last_read_time DESC排序，LIMIT 50条

  库存/状态更新：
  - 代码中无借阅库存管理逻辑，仅有updateBookStatus()方法修改status字段（未在查询中使用）
  - 无逾期判断、预约等功能

  登录/未登录兼容：
  - getBookDetails()接口判断token是否存在（LibraryController.java:36-60）
  - 已登录：调用getBookDetailsByIdWithUser()，通过LEFT JOIN返回收藏状态和阅读次数
  - 未登录：调用getBookDetailsById()，仅返回基础图书信息

---
  4. 潜在亮点（优化点与问题解决）

  数据库优化：
  - ✅ 索引优化：在category_id字段创建idx_category_id索引（ad.sql:40），加速分类查询
  - ✅ 唯一索引：category表的name字段有唯一索引uq_category_name，防止分类重复

  并发与去重处理：
  - ✅ 防止重复收藏：先调用isFavorite()检查，再执行INSERT（LibraryController.java:83）
  - ✅ 阅读历史去重：使用ON DUPLICATE KEY
    UPDATE机制，user_id+book_id作为复合主键自动去重（LibraryMapper.java:86-93）
  - ⚠️ 无事务管理：收藏操作未使用@Transactional，高并发时可能出现问题

  用户体验优化：
  - ✅ 未登录友好：支持未登录用户浏览图书详情，仅收藏/历史需登录
  - ✅ 静默记录：阅读历史记录失败不弹窗提示，不影响用户体验（BookDetailActivity.java:179-180）
  - ✅ 历史数量限制：查询阅读历史LIMIT 50条，防止数据过载（LibraryMapper.java:82）

  代码规范：
  - ✅ 软删除实现：所有查询都过滤del_flg=0，避免硬删除数据
  - ✅ 字段映射清晰：使用AS别名映射数据库字段到驼峰命名（如book_name AS bookName）
  - ⚠️ 未使用缓存：高频查询（如分类列表）未使用Redis等缓存，依赖数据库

  未实现功能：
  - ❌ 无图书借阅、归还、续借功能
  - ❌ 无逾期判断与罚款计算
  - ❌ 无库存数量管理
  - ❌ 无推荐算法（基于阅读历史的个性化推荐）